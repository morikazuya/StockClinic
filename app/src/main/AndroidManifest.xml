<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.km.android.stockclinic">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".trade.PortfolioActivity"></activity>
        <activity android:name=".trade.PieChartActivity" />
        <activity android:name=".trade.StockListActivity" />
        <activity
            android:name=".trade.__TradeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".trade.AddTradeActivity" />
        <activity android:name=".trade.TradeListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>